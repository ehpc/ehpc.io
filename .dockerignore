# Vite + WASM project dockerignore
# Exclude dependencies and build outputs
node_modules
dist
build
coverage

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# TypeScript incremental build info
*.tsbuildinfo

# Local env files (keep only what you explicitly copy)
.env*
!.env.example

# OS / editor junk
.DS_Store
.idea
.vscode

# Rust WASM (wasm-pack / cargo) build artifacts
debug/
target/
pkg/
wasm-pack.log

# If generating .wasm locally (include source, build in container instead)
*.wasm

# Cache directories
.eslintcache
.cache
vite.cache

# Test artifacts
jest-results.json
reports
*.lcov

# Misc
Thumbs.db
*~
Containerfile
.dockerignore
.husky
